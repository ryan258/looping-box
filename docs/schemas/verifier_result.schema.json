{
  "title": "looping-box.verifier-result.v1",
  "type": "object",
  "required": ["schema", "review_id", "generated_at", "status", "checks"],
  "properties": {
    "schema": {"const": "looping-box.verifier-result.v1"},
    "review_id": {"type": "string"},
    "generated_at": {"type": "string"},
    "status": {"enum": ["passed", "failed"]},
    "checks": {
      "type": "array",
      "items": {
        "type": "object",
        "required": ["name", "status", "message"],
        "properties": {
          "name": {"type": "string"},
          "status": {"enum": ["passed", "failed"]},
          "message": {"type": "string"}
        }
      }
    }
  }
}
